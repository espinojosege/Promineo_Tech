/*function greet (name, owner) {
    //if else to compare
    let msg
    if (name === owner) {
        msg = "Hello boss"
    } else {
        msg = "Hello guest"
    }

    return msg;
}

console.log(greet("jose", "notJose"))*/

function stringToArray(string) {
    split()
}